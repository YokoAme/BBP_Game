using UnityEngine;
using UnityEngine.EventSystems;

public class ClickCatcher : MonoBehaviour, IPointerClickHandler
{
    public void OnPointerClick(PointerEventData eventData)
    {
    //    PaperCard current = FindObjectOfType<PaperCard>();
    //    if (current != null)
    //        current.CloseCard();
    }
}
